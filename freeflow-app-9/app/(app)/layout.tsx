/**
 * (app) Route Group Layout
 *
 * This layout wraps all routes in the (app) route group.
 * The route group allows organizing routes without affecting URL structure.
 */

export default function AppRouteGroupLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
