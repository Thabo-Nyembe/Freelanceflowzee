version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: freeflow-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # Add other environment variables here or use env_file
    env_file:
      - .env.local
